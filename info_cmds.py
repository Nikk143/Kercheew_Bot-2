import discord


def news():
    msg = """
hello we are currently working on a `<give` and `shop` command
     -the creators Kercheew_#3629, minhbilly#0416 and Nikk#0261"""
    return msg



def help():
  msg = """       
Here are the list of commands 
                                
`<help`
`<bubblewrap` or `<bw`
`<joke`
`<coinflip`
`prefix.change <new prefix>`
`<news`
`<quote`
     
And here are the WIP/Planned commands
                                
`<bal` or `<$`, `<profile` or `<me` and `<meme`
     
thanks for using Kercheew!"""
  return msg